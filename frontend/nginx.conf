server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # The application is a single-page app with client-side routes, so a deep
    # link such as /statement must serve index.html rather than 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
